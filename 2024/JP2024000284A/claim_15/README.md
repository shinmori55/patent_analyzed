複数の検査対象の中から不良品を検出可能な検査装置に適用するための学習用データを生成可能な学習用データ生成方法であって、
（ａ）背景を含む複数の良品のみの画像である良品画像、および、前記背景を含む少なくとも一つの前記不良品のみの画像である不良品画像を取得し、
（ｂ）取得した前記不良品画像から前記背景を除く前記不良品の画像のみを抽出した画像である不良品抽出画像を生成し、
（ｃ）前記良品画像と前記不良品抽出画像とを合成することによって、学習用画像を生成し、
（ｄ）生成した前記学習用画像にアノテーションを付与することによって、前記学習用データを生成する
学習用データ生成方法。
```mermaid
flowchart TD
A[複数の検査対象の中から不良品を検出可能な検査装置に適用するための学習用データを生成可能な]
B[学習用データ生成方法であって、]
C[(ａ）背景を含む複数の良品のみの画像である良品画像、および、前記背景を含む少なくとも一つの前記不良品のみの画像である不良品画像を取得し、]
D[(ｂ）取得した前記不良品画像から前記背景を除く前記不良品の画像のみを抽出した画像である不良品抽出画像を生成し、]
E[(ｃ）前記良品画像と前記不良品抽出画像とを合成することによって、学習用画像を生成し、]
F[(ｄ）生成した前記学習用画像にアノテーションを付与することによって、前記学習用データを生成する]
G[学習用データ生成方法。]
A --> B
B --> G
C --> G
D --> G
E --> G
F --> G
```